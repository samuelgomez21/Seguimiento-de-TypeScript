function display(a: string, b: string): void
{
    console.log(a + b);
}

function display(a:number): void
{
    console.log(a);
}

//EJemplo de sobrecarga de funciones con diferente numero de parametros y tipos con el mismo nombre 